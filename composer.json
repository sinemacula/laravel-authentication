{
    "$schema": "https://getcomposer.org/schema.json",
    "name": "sinemacula/laravel-authentication",
    "type": "library",
    "description": "Stateless contextual authentication primitives (Identity, Principal, Device, Tenant) for Laravel 12 and 13.",
    "keywords": [
        "sine macula",
        "laravel",
        "auth",
        "authentication"
    ],
    "license": "Apache-2.0",
    "authors": [
        {
            "name": "Ben Carey",
            "email": "bdmc@sinemacula.co.uk"
        }
    ],
    "require": {
        "php": "^8.3",
        "ext-hash": "*",
        "ext-mbstring": "*",
        "ext-openssl": "*",
        "firebase/php-jwt": "^7.0",
        "laravel/framework": "^12.40|^13.3"
    },
    "require-dev": {
        "brianium/paratest": "^7.20",
        "doctrine/coding-standard": "^14.0",
        "friendsofphp/php-cs-fixer": "^3.94",
        "infection/infection": "^0.32.6",
        "larastan/larastan": "^3.9",
        "mockery/mockery": "^1.6",
        "orchestra/testbench": "^10.2|^11.0",
        "phpbench/phpbench": "^1.6.1",
        "phpstan/extension-installer": "^1.4",
        "phpstan/phpdoc-parser": "^2.3",
        "phpstan/phpstan": "^2.1",
        "phpstan/phpstan-deprecation-rules": "^2.0",
        "phpstan/phpstan-mockery": "^2.0",
        "phpstan/phpstan-phpunit": "^2.0",
        "phpstan/phpstan-strict-rules": "^2.0",
        "phpunit/phpunit": "^12.5",
        "sinemacula/coding-standards": "^1.0",
        "slevomat/coding-standard": "^8.28",
        "squizlabs/php_codesniffer": "^4.0"
    },
    "autoload": {
        "psr-4": {
            "SineMacula\\Laravel\\Authentication\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Benchmarks\\": "benchmarks/",
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "check": [
            "qlty check"
        ],
        "format": [
            "qlty fmt"
        ],
        "test:mutation": [
            "@php vendor/bin/infection --configuration=infection.json5 --test-framework-options=\"--do-not-fail-on-warning --do-not-fail-on-phpunit-warning\" --only-covering-test-cases --min-msi=85 --min-covered-msi=90"
        ],
        "test:mutation:full": [
            "@php vendor/bin/infection --configuration=infection.full.json5 --test-framework-options=\"--do-not-fail-on-warning --do-not-fail-on-phpunit-warning\" --only-covering-test-cases"
        ],
        "test:perf:contracts": [
            "@php vendor/bin/phpunit --testsuite PerformanceContracts"
        ],
        "bench": [
            "@php vendor/bin/phpbench run benchmarks --config=phpbench.json --progress=plain --report=aggregate"
        ],
        "bench:ci": [
            "@php vendor/bin/phpbench run benchmarks --config=phpbench.json --progress=plain --report=aggregate --dump-file=build/phpbench.xml"
        ],
        "test": [
            "@php vendor/bin/paratest $@"
        ],
        "test-coverage": [
            "@php vendor/bin/paratest --coverage-clover=clover.xml"
        ]
    },
    "scripts-descriptions": {
        "check": "Run static analysis and lint checks via qlty.",
        "format": "Format codebase using qlty.",
        "test:mutation": "Run the scoped Infection mutation-testing gate for the highest-risk auth paths.",
        "test:mutation:full": "Run the full Infection mutation suite across src/ without MSI thresholds.",
        "test:perf:contracts": "Run deterministic query/write budget tests for auth hot paths.",
        "bench": "Run PHPBench benchmarks for runtime and JWT hot paths.",
        "bench:ci": "Run PHPBench benchmarks and dump CI output for artifact upload.",
        "test": "Run the default PHPUnit test suites in parallel using Paratest.",
        "test-coverage": "Shortcut to run Paratest coverage for default suites."
    },
    "extra": {
        "laravel": {
            "providers": [
                "SineMacula\\Laravel\\Authentication\\AuthServiceProvider"
            ]
        }
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true,
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true,
            "infection/extension-installer": true,
            "phpstan/extension-installer": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
